# MQL5 fallback EA (not yet built)

`DelicateTrader.mq5` - a thin transport-only EA, built only if a broker or
prop firm requires order origination from an EA rather than the Python
agent. See `docs/specs/SPEC-04-agent-protocol.md` §9 and
`docs/specs/SPEC-10-build-plan.md` Phase 5b.

Out of scope for the current MVP build - see `docs/adr/0001-mvp-scope.md`.
Nothing here yet.
