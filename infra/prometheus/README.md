# Prometheus scrape config (not yet built)

Metrics per `docs/specs/SPEC-08-infrastructure.md` §5 - none of the workers
that would emit `dt_*` metrics exist yet (see `docs/adr/0001-mvp-scope.md`),
so there's nothing to scrape.
