# Runbooks (not yet written)

`docs/specs/SPEC-08-infrastructure.md` §9 lists ten runbooks to write before
they're needed, `emergency-flatten.md` first because it's the one that
matters most under pressure. None are written yet - there is no live agent,
broker connection, or execution path for them to document (see
`docs/adr/0001-mvp-scope.md`). Write these starting in Phase 5 (MT5 bridge),
not before - a runbook for a system that doesn't exist yet is fiction.
