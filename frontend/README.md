# Terminal (Next.js) - not yet built

Built last, per `docs/specs/SPEC-09-frontend.md`: "the system must be fully
operable through the API and logs before a pixel is drawn, so that the UI
is never load-bearing for safety." See `docs/specs/SPEC-10-build-plan.md`
Phase 7.

Out of scope for the current MVP build - see `docs/adr/0001-mvp-scope.md`.
Nothing here yet.
